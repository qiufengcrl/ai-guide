---
description: 浏览器、录屏操作、看录屏都走 Grok（inherit），禁止改用 Claude 或其他模型
alwaysApply: true
---

# 浏览器 / 录屏 / 看录屏 = Grok bot

当前 Cloud Agent 主模型是 Grok 时，下列能力一律视为 **Grok bot**，跟父会话同一模型：

| 能力 | 做法 |
|------|------|
| 浏览器操作 `computerUse` | `Task` + `subagent_type: computerUse`，`model` 省略或 `inherit` |
| 录屏 `RecordScreen` | 只负责开始/保存/丢弃；期间的点击、输入仍由上面的 `computerUse`（Grok）完成 |
| 看录屏 `videoReview` | `Task` + `subagent_type: videoReview`，`model` 省略或 `inherit` |

硬性约束：

- **必须**省略 `model`，或显式 `inherit`（与父会话 Grok 一致）
- **禁止**自行传入 `claude-4.5-sonnet`、`claude-sonnet-5`、平台默认视觉模型或其他非 inherit 型号
- 只有用户在**本会话里点名**某个允许的模型时，才可以改 `model`
- 不要把 `videoReview` 当成「专用视觉模型」而换掉 Grok
